# --- Base de datos (PostgreSQL) ---
DB_HOST=localhost
DB_PORT=5432
DB_NAME=plataforma_educativa
DB_USER=postgres
DB_PASSWORD=cambia_esto

# --- Autenticación ---
JWT_SECRET=cambia_esto_por_un_secreto_largo_y_aleatorio
JWT_EXPIRES_IN=7d

# --- Servidor ---
PORT=4000

# --- Correo (para enviar el código ALU-XXXXX) ---
SMTP_HOST=smtp.tuservidor.com
SMTP_PORT=587
SMTP_USER=notificaciones@tuplataforma.com
SMTP_PASSWORD=cambia_esto
